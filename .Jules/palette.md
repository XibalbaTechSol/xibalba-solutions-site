## YYYY-MM-DD - Explicit Form Label Pairings
**Learning:** Found an accessibility issue where form labels in `contact.html` lacked explicit `for`/`id` associations, reducing the click target size and breaking screen reader context.
**Action:** Ensure all form labels have explicit `for` attributes that match the `id` of their respective input elements to ensure optimal accessibility.
