## 2026-05-31 - Explicit Form Labels
**Learning:** Inconsistent usage of `for` and `id` attributes on form fields causes accessibility issues for screen readers.
**Action:** Always verify and implement explicit pairings for labels and inputs (`<label for="x">` and `<input id="x">`) across all forms.
