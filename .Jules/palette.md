## 2025-02-23 - Form Submission Feedback
**Learning:** Standard HTML forms with `action` attributes (like Formspree) often lack built-in submission feedback, leading to user uncertainty. Adding a global JS interceptor for `form[action]` to toggle button state is a robust pattern for static sites.
**Action:** Apply this pattern to all static forms to ensure consistent feedback.
