## 2026-05-28 - Form Label Accessibility
**Learning:** Found forms without explicit `<label for="x">` and `<input id="x">` pairings, which impairs screen reader navigation and click-to-focus behavior.
**Action:** Always ensure inputs have associated label `for` attributes linked to the input's `id`.
