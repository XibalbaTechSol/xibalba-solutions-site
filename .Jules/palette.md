## 2026-06-24 - Explicit Form Label Pairings
**Learning:** Found an accessibility pattern where form labels were visually associated with inputs but lacked programmatic pairing (missing `for` and `id` attributes), reducing click targets and screen reader accessibility.
**Action:** Always ensure explicit `for` and `id` pairings are used across all forms to improve screen reader context and usability.
