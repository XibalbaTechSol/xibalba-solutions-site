## 2026-06-30 - [Explicit Form Labels]
**Learning:** Forms in this app frequently lack explicit `<label for="x">` and `<input id="x">` bindings, impairing screen reader context.
**Action:** Always ensure form inputs and their labels are explicitly paired using `for` and `id` attributes.
