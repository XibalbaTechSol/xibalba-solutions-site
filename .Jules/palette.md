## 2026-06-22 - Explicit Form Pairings
**Learning:** Found forms without explicit `for` and `id` pairings. This pattern negatively impacts screen reader compatibility and click targeting.
**Action:** Always ensure explicit `<label for="id">` and `<input id="id">` pairings are used for all forms.
