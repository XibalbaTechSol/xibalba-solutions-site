## 2024-05-17 - [Synchronized ARIA state on mobile navigation]
**Learning:** Custom mobile navigation toggles need programmatic synchronization of `aria-expanded` attributes with CSS state classes for screen reader users to understand the current visibility of the menu.
**Action:** Always verify `aria-expanded` is toggled alongside `active` classes on hamburger buttons.
